syntax = "proto3";

package MF.V3.Settings;

/// Turntable settings.
message Turntable
{
    /// Use the turntable.
    optional bool use = 1;

    /// The number of turntable steps.
    int32 steps = 2;

    /// Turntable angle sweep in degrees.
    int32 sweep = 3;
}
